#include <stdio.h>
#include <string>
#include <iostream>

int main1106() {
    char a[1000] = { 0 };
    char *b = "5";
    char *p;
    while (~scanf("%s", a)) {
        p = strtok(a, b);
        while (p != NULL) {
            
        }
    }

    return 0;
}